//import {ObjectId, WithId} from "mongodb";

export type BlogDBType = {
    id: string
    name: string
    description: string
    websiteUrl: string
}

// const BlogDBTypeWithID = ObjectId<BlogDBType>

