export type LoginOutputServiceType = {
    accessToken: string
    refreshToken: string
}

export type RefreshTokenOutputServiceType = {
    accessToken: string
    refreshToken: string
}

export type LogoutOutputServiceType = {
    accessToken: string
    refreshToken: string
}